# System_Monitor

This Python script monitors CPU, memory, and disk usage and sends alerts via Telegram if thresholds are exceeded.  

Features:  Monitors CPU, memory, and disk usage. Sends Telegram alerts when usage exceeds 80% (CPU and memory) or 85% (disk).  

Requirements:  Python 3.x, psutil, curl, Replace bot_token and chat_id with your own values.
